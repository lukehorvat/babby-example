{
  "private": true,
  "scripts": {
    "build": "rimraf dist && babel lib -d dist",
    "prepublish": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "babby": "0.0.1",
    "babel-plugin-transform-shorthand-instance-expressions": "0.0.1",
    "babel-preset-es2015": "6.6.0",
    "rimraf": "2.5.2"
  },
  "babel": {
    "plugins": [
      "transform-shorthand-instance-expressions"
    ],
    "presets": [
      "es2015"
    ]
  }
}
